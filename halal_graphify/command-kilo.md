---
description: Build or query a halal-graphify knowledge graph
---

Invoke the `halal-graphify` skill immediately.

Pass the full `/halal-graphify` argument string through unchanged.
If no arguments were supplied, treat the target path as `.`.

Examples:
- `/halal-graphify`
- `/halal-graphify src --update`
- `/halal-graphify query "what connects auth to billing?"`

Do not answer from raw files before handing off to the `halal-graphify` skill.
